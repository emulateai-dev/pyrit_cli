services:
  phoenix:
    image: arizephoenix/phoenix:latest
    ports:
      - "16007:6006" # PHOENIX_PORT
      - "24317:4317" # PHOENIX_GRPC_PORT
    environment:
      - PHOENIX_WORKING_DIR=/mnt/data
    volumes:
      - phoenix_data:/mnt/data

volumes:
  phoenix_data:
